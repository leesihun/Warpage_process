# Data Auto Transfer Configuration
# Lines starting with # are comments

# Schedule Configuration
SCHEDULE_TIME=06:00
SCHEDULE_TIMES=06:00, 18:00
TIMEZONE=Asia/Seoul

# Source Configuration
SOURCE_DIRECTORY=../data
FOLDER_PATTERN=%Y%m%d

# Target Configuration
TARGET_IP=10.252.38.241
TARGET_DIRECTORY=gumi_s-aoi
PROTOCOL=smb
USERNAME=s.hun.lee@samsung.com
PASSWORD=qwerty12!
PASSWORD_FALLBACKS=atleast12!
SSH_KEY_PATH=

# Transfer Options
DRY_RUN=false
RETRY_ATTEMPTS=3
RETRY_DELAY=30
DELETE_AFTER_TRANSFER=false
LOG_LEVEL=INFO

# Protocol Options:
# - ssh: Use SSH/SFTP via paramiko (recommended - no system SSH required)
# - scp: Use SSH/SFTP via paramiko (alias for ssh)
# - smb: Use SMB/CIFS with automatic connection cleanup
# - local: Copy to local directory
#
# Note: SSH protocol uses paramiko library and doesn't require system SSH installation
# For network requirements: SSH uses port 22, SMB uses ports 445/139