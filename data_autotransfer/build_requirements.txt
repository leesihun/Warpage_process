# Build Requirements for Data Auto Transfer System
# Install these before building the executable

# Core PyInstaller for building exe
pyinstaller>=5.0.0

# Runtime dependencies (already in requirements.txt)
schedule>=1.2.0
paramiko>=2.11.0
smbprotocol>=1.10.0

# Optional but recommended for better compression
upx-ucl  # UPX compressor (if available on system)